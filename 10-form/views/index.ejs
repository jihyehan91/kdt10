<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Form 전송</title>
</head>

<body>
  <h1>Form</h1>

  <h2>Get 요청</h2>

  <form action="/login" method="get">
    <label for="id">ID</label>
    <input type="text" name="id" placeholder="ID는 최소 3글자" id="id" required minlength="3">
    <label for="pw">
      PW
      <input type="password" name="pw" placeholder="PW" id="pw" required>
      <!-- label의 for 속성과 input의  id 속성 일치시켜야 작동 -->
    </label>

    <input type="number" name="grade" placeholder="GRADE" max="4">
    <!-- form 태그 안에 있는 button 태그는 type: submit(기본값) -->
    <button>제출</button>
    <!-- <button type="button">제출</button> -->
  </form>

  <!-- Post 요청 -->
  <h2>Post 요청</h2>
  <!-- pattern 속성: 폼 요소에 값이 입력되었을 때, 그 입력된 값에 대해 검증 -->
  <!-- required 속성: 폼 요소에 값이 반드시 들어가야 하는지 여부를 결정 -->

  <form action="/login" method="post">
    <!-- 메서드가 다르면 url이 같아도 상관없음 -->
    <label for="id">ID</label>
    <input type="text" name="id" placeholder="ID" id="id" pattern="^([a-zA-Z0-9가-힣]){4,10}$" title="아이디는 대소문자, 숫자, 한글로 구성된 4자 이상의 문자여야 합니다." required>
    <!-- name은 키값 > id2로 입력하면 서버에서 {id2:''} 형식으로 출력 -->
    <label for="pw">
      PW
      <input type="password" name="pw" placeholder="PW" id="pw" pattern="^([a-z0-9]{8,12})" required>
      <!-- label의 for 속성과 input의  id 속성 일치시켜야 작동 -->
    </label>
    <!-- form 태그 안에 있는 button 태그는 type: submit(기본값) -->
    <button>제출</button>
    <!-- <button type="button">제출</button> -->
  </form>

  <h2>JS 유효성 검사</h2>
  <!-- js form 유효성 검사 -->
  <form action="/js-form-check" method="post" name="valiForm" onsubmit="return checkForm(this)" target="_blank">
    <input type="text" name="id" minlength="1" placeholder="ID" title="최소 1글자">
    <span>우편물 수령장소</span>
    <input type="checkbox" name="post" value="자택">자택
    <input type="checkbox" name="post" value="회사">회사
    <button>제출</button>
  </form>

</body>

</html>